# Frontend :
-> For authorization.
I changed AuthUser.js, Form.jsx, Header.jsx, App.jsx(Route)

# Backend :
-> For authorization.
I changed Invoice Controller -> Login function





const [inputList, setInputList] = ({fName,lName});

const handleInputChange = (e,index)=>{
const {name,value} = e.target;
const list = [...inputList];
list[index][name] = value;
setInputList(list);
}

const handleAddClick = () => {
setInputList(...inputList, {fName:"",lName:"'});
}

const handleRemoveClick = index=>{
  list[...inputList];
  list.splice(index,1);
  setInputList(list);
}

{ inputList..map((x,i)=>{
return(
  <div>
<input type="text" name="fName" onChange={e=>handleInputChange(e,i)}/>

<input type="text" name="lName" onChange={e=>handleInputChange(e,i)}/>

{
  inputList.lenght !==1 && 
  <button onClick="()=>handleRemoveClick(i)">Remove</button>
}
{
  inputList.lenght -1==i && 
  <button onClick="handleAddClick">Add</button>
}
</div>
)
})}